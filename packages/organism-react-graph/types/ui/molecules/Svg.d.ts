export default Svg;
declare function Svg(props: any): JSX.Element;
declare namespace Svg {
    namespace defaultProps {
        const ui: boolean;
        const atom: string;
        const width: string;
    }
}
